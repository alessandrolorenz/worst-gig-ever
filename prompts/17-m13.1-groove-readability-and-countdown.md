# Execute M13.1 — Groove Readability & Entry Tuning

You are continuing the existing **Worst Gig Ever** rhythm-pivot branch after the first physical M13 playtest.

## Owner feedback

The owner has physically played the rhythm build.

Observed:

- Groove + Defense is difficult but fun.
- The challenge itself should be preserved.
- The current Groove Pad requires too much visual attention away from incoming bottles.
- The owner wants the Groove Pad larger and lower-center, integrated with the drum kit.
- The owner wants a `3 → 2 → 1 → GO` countdown before active gameplay.
- A second Groove area is a future difficulty idea only.

## Read first

1. `AGENTS.md`
2. `START-HERE.md`
3. `project-status.md`
4. all current rhythm-pivot ADRs/specs
5. `docs/architecture/rhythm-pivot-architecture.md`
6. `docs/specs/M13.1-groove-readability-and-entry-tuning.md`
7. `docs/verification/M13.1-test-matrix.md`
8. `docs/verification/M13.1-gate.md`
9. current round state / rhythm domain / input routing / renderer / HUD / tests

## Reconcile actual repository state

Before changes:

- record current branch;
- record HEAD;
- run `git status`;
- inspect recent commits.

The earlier M13 report ended at branch `m9/rhythm-pivot`, HEAD `729014d`, but the actual repo is the source of truth.

Do not reset or discard newer valid owner work.

## Implementation requirements

Implement the M13.1 spec exactly.

### Groove Pad

- Move it to the lower-center visual focus area.
- Make it meaningfully larger.
- Integrate it visually with the drum-kit foreground.
- Keep pad geometry config-driven.
- Preserve surface-relative input mapping.
- Do not introduce nested Pressables.
- Keep pulse visual layers non-interactive.
- Preserve overlapping pad+target dual resolution.

### Countdown

Implement one coherent:

`3 → 2 → 1 → GO`

pre-roll using the existing deterministic visual beat clock.

Important:

- replace the prior 2-beat count-in behavior;
- do not stack both;
- no target spawning before GO;
- no Groove scoring/misses before GO;
- no Integrity consequence before GO;
- no vocalist timeline progression before GO;
- GO itself is not a scored beat;
- first scored Groove beat is one beat after GO.

### Lifecycle

- backgrounding during countdown cancels it and returns to READY;
- normal PLAYING auto-pause remains unchanged;
- restart starts a fresh countdown.

### Difficulty freeze

Do not change:

- BPM;
- timing windows;
- target speeds;
- spawn cadence;
- fastball behavior;
- target hit forgiveness;
- Integrity;
- vocalist timing;
- round duration;
- Groove score values;
- Defense score values.

Do not implement a second Groove Pad.

## Tests

Add/update deterministic tests required by the M13.1 spec and test matrix.

Pay special attention to:

- countdown replacing the old count-in rather than adding to it;
- frame-rate independence;
- first scored beat offset after GO;
- target schedule starting only after active gameplay begins;
- multi-touch / overlapping target+pad input regressions.

## Verification

Execute `docs/verification/M13.1-gate.md` in full.

Fix objective failures before declaring completion.

## Documentation

Update:

- `project-status.md`;
- M13 physical-playtest status/decision as appropriate;
- relevant architecture notes;
- ADR only if implementation introduces a meaningful long-lived architectural decision.

Do not rewrite unrelated historical docs merely to make wording prettier.

## Commit

If and only if the full M13.1 gate passes, create local commit:

`feat: improve groove readability and pre-round countdown`

Do not push.
Do not run EAS.

## Return report

Return:

1. incoming branch / HEAD / worktree;
2. owner-feedback interpretation;
3. exact files changed/created/deleted;
4. previous vs new Groove Pad geometry;
5. countdown state/timing model;
6. explanation of how the old 2-beat count-in was reconciled;
7. explicit difficulty-freeze old/new values;
8. tests added/updated;
9. full automated verification results;
10. Android/native smoke result if available;
11. known limitations;
12. final branch / HEAD / git status;
13. commit SHA;
14. explicit confirmation:
    - no push;
    - no EAS;
    - no second pad;
    - no difficulty values changed.

Final milestone status must be:

`READY_FOR_M13_1_RETEST`
