# Execute M6 — Art Direction Lock

Read `AGENTS.md`, `project-status.md`, M3, M5A, M6, ADR 0007/all active ADRs, current asset manifest, and current stage/rendering code.

Required work:
1. Confirm actual runtime presentation states for vocalist/bassist/guitarist/crowd.
2. Reconcile M3 with M6: preserve valid M3 decisions, update stale parts, avoid contradictory contracts.
3. Confirm exact Pack 1 naming, layering, dimensions, transparency and required/optional status.
4. Confirm `idle`/`loopA`/`loopB`/`hitReaction`/`dodge` visual mapping.
5. Confirm how M5A throw arcs constrain composition/safe flight space.
6. Create/update art provenance from the supplied template.
7. Update `project-status.md` factually.
8. Do not create final art and do not change gameplay.

Verification: execute `docs/verification/M6-gate.md`.

If green, local commit: `docs: lock M6 art direction`
Do not push.

Return incoming/outgoing HEAD, files changed, M3 reconciliation, frozen Pack 1, animation mapping, gate results, commit SHA, git status.
